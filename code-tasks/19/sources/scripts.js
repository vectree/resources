// получаем число на вход - number
function run (number) {
  if (number == 6) {
    // возвращаем true из функции
    return true;
  } else {
    // возвращаем false из функции
    return false;
  }
}